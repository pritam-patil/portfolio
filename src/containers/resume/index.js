import React from 'react';
import { Education, Skills, Work } from './components';

const Resume = () => (
  <section id="resume">
    <Education />
    <Work />
    <Skills />
  </section>
);

export default Resume;